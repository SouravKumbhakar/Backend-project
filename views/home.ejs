<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white px-12 py-10">
        <a class="border-2 border-zinc-700 absolute right-10 bg-red-500 py-2 px-3 text-sm rounded-md" href="/logout">logout</a>
        <h3 class="text-3xl border-b-2 border-yellow-300 w-[100px]">Feed</h3>
        <p class="mt-3 text-xl font-light">Hello, <span class="text-blue-200 font-normal">user</span> 👋🏻</p>
        <h5 class="text-blue-500 mt-5 mb-2">You can create a post</h5>
        <form action="/post" method="post">
            <textarea class="resize-none bg-transparent border-2 border-zinc-800 outline-none p-3 rounded-md w-2/3" name="content" placeholder="What's on your mind ?"></textarea>
            <input class="px-3 py-2 rounded-md bg-blue-500 w-40 block mt-3" type="submit" value="Create new post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your posts</h3>
            <div class="postcontainer mt-5">
                <% allposts.reverse().forEach( function(post){ %>
                    <div class="post w-1/3 p-5 mb-4 bg-zinc-800 border-2 border-zinc-700 rounded-md">
                        <h4 class="text-green-400"><%=post.user.username%></h4>
                        <p class="text-[12px] text-zinc-500 mb-2"><%= post.date.toDateString() %></p>
                        <hr>
                        <p class="mt-2 text-sm tracking-tight"><%= post.content %></p>
                        <div class="btns flex mt-5 gap-3">
                            <a class="text-blue-500" href="/like/<%= post._id%>"><%= post.likes.length %> <%= post.likes.indexOf(post.user._id) == -1? "like" : "unlike" %></a>
                            <a class="text-zinc-400" href="/edit/<%= post._id%>">Edit</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>